(()=>{const root=document.documentElement;const reduce=matchMedia('(prefers-reduced-motion: reduce)').matches;const fine=matchMedia('(pointer:fine)').matches;const reveals=[...document.querySelectorAll('.reveal')];if('IntersectionObserver'in window&&!reduce){const io=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('in');io.unobserve(e.target)}}),{threshold:.12,rootMargin:'0px 0px -7%'});reveals.forEach(el=>io.observe(el))}else reveals.forEach(el=>el.classList.add('in'));const toggle=document.querySelector('.menu-toggle');const drawer=document.querySelector('.mobile-drawer');if(toggle&&drawer){toggle.addEventListener('click',()=>{const open=drawer.classList.toggle('open');toggle.setAttribute('aria-expanded',String(open))});drawer.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>{drawer.classList.remove('open');toggle.setAttribute('aria-expanded','false')}))}if(!reduce&&fine){let tx=0,ty=0,cx=0,cy=0,raf=0;const stage=document.querySelector('.hero-stage');const windowEl=document.querySelector('.product-window');const tick=()=>{cx+=(tx-cx)*.08;cy+=(ty-cy)*.08;root.style.setProperty('--mx',`${50+cx*22}%`);root.style.setProperty('--my',`${28+cy*18}%`);if(stage&&windowEl){const rtl=document.documentElement.dir==='rtl'?-1:1;windowEl.style.transform=`rotateY(${rtl*(5+cx*2.6)}deg) rotateX(${2-cy*2.2}deg) translate3d(${cx*5}px,${cy*4}px,0)`}raf=requestAnimationFrame(tick)};addEventListener('pointermove',e=>{tx=e.clientX/innerWidth*2-1;ty=e.clientY/innerHeight*2-1},{passive:true});raf=requestAnimationFrame(tick);addEventListener('pagehide',()=>cancelAnimationFrame(raf),{once:true})}const metricObserver=!reduce&&'IntersectionObserver'in window?new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('metric-live');metricObserver.unobserve(e.target)}}),{threshold:.5}):null;document.querySelectorAll('.product-window').forEach(el=>metricObserver?metricObserver.observe(el):el.classList.add('metric-live'))})();